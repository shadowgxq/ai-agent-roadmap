# 2026-09-15

- W18 Session 4 核心代码已提交 `92f3703`：DAG 与关键路径、只读并行、受控工具预算、attempt 聚合、同角色改派，以及超时旧 Worker 占用与工作区隔离。未运行测试或验证命令，按用户要求继续 S5/S6 各一个 commit。

- 完成公司研究 AI 内核技术选型文档：`outputs/AI公司研究内核_技术架构与选型_v0.1.md`。审阅 support-agent、advanced-coding-agent、agent-mini 和原生搜索 demo，并固定参考 ai-berkshire 提交 `43fbaed85af8b88ce233b263e7c76c8a953ae380`。
- 推荐当前仓库独立 research-agent 模块：LangGraph 编排，LangChain 模型适配，版本化 Skill，原生搜索适配器，独立证据/计算/审计仓库与 PostgreSQL checkpoint。先标准研究闭环，再团队和管理层专项；不包含前后端建设。
- 重要迁移边界：上游文字双源规范与脚本默认阈值不一致；原生搜索 API 内部步骤不等于图 checkpoint 粒度；已结束 partial 需显式补跑而非普通 continue。仅静态审阅与文档产出，未改业务代码或运行测试。
- 用户继续选择细化技术文档，新增 `outputs/AI公司研究内核_P0详细设计_v0.2.md`：明确节点读写、原生搜索交换/续接、reserved/in_flight/unknown 状态、预算结算、报告候选—审计回执—交付清单，以及已有观测纠错回流。P0 简版是建议实施子集，不替代完整五年研究需求；具体模型、预算和双源政策仍待确认。
- 用户确认文档拆分方向后完成两份正式文档：`outputs/AI-Agent通用模板_技术设计与实现规范_v1.0.md`（公共 Runtime）和 `outputs/AI公司研究_业务架构与接入设计_v1.0.md`（ai-berkshire 业务接入）。决策为 DeepSeek 原生搜索基线、先 standard_p0、官方原文+独立复核、预算 hard/soft 尚未定；仅静态文档产出，未修改业务代码或运行测试。

- W18 Session 5 已提交 `ba19722`：结构化 claim/evidence/独立验证绑定，保留未解决冲突，有限追加调查与人工 review，Coder 写入前 gate；缓存/数据库冲突仅为声明的 fixture，未运行。
- W18 Session 6 已提交 `a28aafc`：复用 Session 1 冻结的 10 个任务，校验配对观测指纹/快照/验收版本；比较成功率、成本、wall-clock、工具、context 与重复副作用，提供 W17/W18 观测适配和离线 CLI。缺失数据保持 null/insufficient_data，不生成实测结论。
- W18 剩余三个 Session 均只实现核心代码，各一个本地 commit；遵守不运行测试/验证/服务的选择，未 push；无关 frontend 改动未纳入提交。线程调度的权限和预算保证限于受控工具入口，不能强杀运行中的线程或代替进程级隔离。
- W18 Session 4–6 收尾完成：交付包 `outputs/W18-Sessions-04-06-a28aafc.zip` 已包含 16 个已提交文件（含冻结任务集），工作区仅保留原有 frontend 无关改动，未运行测试/验证命令。

- 完成两份 v1.0 文档的交叉 review（对照 v0.1/v0.2）。用户点名要补的 6 项 LangGraph 细节覆盖情况：错误边界/Langfuse/Checkpoint/Plan/Multi-Agent 五项完整，重试一项缺 `langgraph>=1.2` 版本门槛与节点级 `timeout=`/`error_handler=` 代码形态。
- Review 发现的实质退化（v1.0 相对 v0.1/v0.2）：公共模板丢失「安全与外部资料处理」章节和「技术选型与版本策略」章节；业务文档丢失 v0.2 的 P0 节点表/状态机、原生搜索适配器字段协议（NativeExchangeSpec/NativeStepResult）、RequestContract、Operation/BudgetReservation 台账、恢复时序图、依赖失效矩阵、审计检查项清单。两份 v1.0 都缺 CapabilityProfile 字段、事件协议、公共 API 清单。
- 已核实的源码引用准确度：`graph.py:249` 属性遮蔽、`:300` 方法定义、`:281` reactive、`:1305` add_node、`recovery.py:534` assert_recovery_consistency、`loop.py:1021/1055`、`runtime.py:273/307`、`checkpointer.py` 19 行、`runner.py` 96 行均属实。仅 `graph.py` 注册行号标注 1306 而实际 1305（off-by-one）；同文件多处的行号范围（全文件 vs 局部）未标注，易被误读为矛盾。
- 本轮仅 review，未修改任何文档内容。

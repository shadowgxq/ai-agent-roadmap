# Used by: advanced-coding-agent --mode planning --planner-backend llm
# The endpoint may be an OpenAI-compatible /v1 URL or a full
# /chat/completions URL.
AGENT_MODEL=
AGENT_API_KEY=
AGENT_BASE_URL=https://api.openai.com/v1

[project]
name = "agent-mini"
version = "0.1.0"
description = "A minimal coding agent built for the AI Agent roadmap"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.68.0",
    "fastapi>=0.115.0",
    "mcp>=2,<3",
    "openai>=2.44.0",
    "pydantic-settings>=2.10.1",
    "pyyaml>=6.0.3",
    "uvicorn>=0.34.0",
]

[tool.uv]
package = false

[project]
name = "mcp-server-playground"
version = "0.1.0"
description = "Minimal MCP server for the W08 learning exercises"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2,<3",
]

[tool.uv]
package = false
